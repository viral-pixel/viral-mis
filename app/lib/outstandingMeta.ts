// "Outstanding Payment Tracker" sub-module (Sandip Reports module). Sandip
// uploads a finished Excel; the app stores and shows it, never computes.
export const OUTSTANDING_SUBMODULE_SLUG = "sandip-outstanding-tracker";
